{
  "name": "@shadregistry/cli",
  "version": "0.2.2",
  "description": "CLI for publishing and managing shadcn-compatible component registries on ShadRegistry",
  "type": "module",
  "bin": {
    "shadregistry": "./dist/bin/shadregistry.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts ./bin/shadregistry.ts --outdir ./dist --target node --format esm --splitting",
    "dev": "bun run ./bin/shadregistry.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "preversion": "bun run typecheck && bun run test",
    "version": "bun run build",
    "postversion": "git push origin main --tags",
    "release": "npm version patch",
    "release:minor": "npm version minor",
    "release:major": "npm version major"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ShadRegistry/cli.git"
  },
  "homepage": "https://shadregistry.com",
  "bugs": {
    "url": "https://github.com/ShadRegistry/cli/issues"
  },
  "keywords": [
    "shadcn",
    "registry",
    "components",
    "cli",
    "publish"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.1",
    "ora": "^8.1.0",
    "open": "^10.1.0",
    "zod": "^3.23.8",
    "fast-glob": "^3.3.3",
    "diff": "^7.0.0"
  },
  "devDependencies": {
    "@types/diff": "^6.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
